# this is only for search engines, it does not appear in the home
# as long as the json variables name and description are provided
title: About me
description: self introduction using json
baseurl: ""
url: https://about.dengzexuan.top

# this information is shown on the homepage, all fields are optional and
# can be declared either as a single string or as a string array/hash
json:
  name: Zexuan Deng
  description:
    - Hello!
    - This is DZX
    - Looks like json
    - Funny, isn't it?
  # cv: assets/cv.pdf
  address:
    - NanJing University
    - Computer Science
  contact:
    email: dzx60090@gmail.com
  profiles:
    blog: https://dengzexuan.top
    github: https://github.com/umnooob

exclude:
  - LICENSE
  - README.md

plugins:
  - jekyll-seo-tag
  - jekyll-sitemap

whitelist:
  - jekyll-sitemap

sass:
  sass_dir: _sass
  style: compressed
  sourcemap: never
